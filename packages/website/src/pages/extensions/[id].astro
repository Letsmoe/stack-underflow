---
import Layout from "../../layouts/Layout.astro";

const { id } = Astro.props;

// Check if an application actually registered a WebView with the given id.
---

<Layout title={"I have to reimplement this fucking thing... Again!"}>
	{id}
</Layout>